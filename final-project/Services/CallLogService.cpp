#include "CallLogService.h"

#include <iostream>
#include <optional>
using namespace std;

void CallLogService::Run() {
    char user_choice = '0';
    cout << "Press Enter to load call logs data:" << endl;
    std::cin.get();
    exit(EXIT_SUCCESS);
}
